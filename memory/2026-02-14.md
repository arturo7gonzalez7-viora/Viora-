[2026-02-14] — Pre-compaction durable memory flush

Events saved:
- Investigated repeated "API provider returned a billing error" messages in Discord #viora.
- Found OpenAI API keys present in /root/.openclaw/.env (multiple keys) and Anthropic used as primary; system was sometimes falling back to OpenAI which triggered the billing notices.
- Emergency mitigation: removed OpenAI from model fallbacks temporarily and restarted the gateway so the bot would stop falling back and spamming billing messages. Primary switched to Anthropic (Claude Sonnet 4.5) and Claude Opus set as fallback.
- User wants to prefer OpenAI (cheaper) and cut Anthropic usage; user listed multiple OpenAI keys and confirmed they have ~$17 credits in OpenAI.
- User chose Claw 3 (sk-...1WIA) as the OpenAI key to use as primary. Assistant requested the full key to write into /root/.openclaw/.env and run the switch/test; key not yet provided.
- Next planned steps (pending user secret): make Claw 3 the only OPENAI_API_KEY, set default model to openai/gpt-5-mini, add retry/backoff, restart gateway, run one safe test call, restore safe fallbacks and suppress noisy billing alerts. Will report exact logs and changes after running.

Notes & preferences:
- User asked for short, simple instructions and minimal text when taking action.
- If the user does not paste the key, assistant will not change configs.

Source: session activity 2026-02-14 (main session)